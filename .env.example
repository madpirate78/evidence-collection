# Database (Neon PostgreSQL)
# Pooled connection string (for application queries)
DATABASE_URL=postgresql://USER:PASSWORD@ep-xxxxx-xxxxx-pooler.us-east-2.aws.neon.tech/neondb?sslmode=require
# Direct connection string (for migrations — bypasses pgbouncer)
DIRECT_DATABASE_URL=postgresql://USER:PASSWORD@ep-xxxxx-xxxxx.us-east-2.aws.neon.tech/neondb?sslmode=require

# Security
SESSION_TIMEOUT=1800
RATE_LIMIT_REQUESTS=10
RATE_LIMIT_WINDOW=900

# Iframe embedding - set to allow embedding from a specific domain
# ALLOWED_EMBED_ORIGIN=example.com
